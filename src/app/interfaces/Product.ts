export interface IProduct {
    id?: string,
    name?: string,
    images?: string[],
    price?: number,
    stock?: number,
    description?: string,
    slug?: string,
    category?: string,
    createdAt?: string,
    updatedAt?: string
}